const React = require('react')

const App = React.createClass({
    render() {
        return (
            <div>
                <h1>Test of React Page</h1>
            </div>
        )
    }
})

module.exports = App
